/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["files.stripe.com", "placehold.co", "images.unsplash.com", "plus.unsplash.com"],
  },
}

module.exports = nextConfig
